apiVersion: v2
name: bakery
description: >
  Crumb & Ember bakery microservices platform — one chart deployed with a
  different values-<env>.yaml per stage (dev, uat, cug, prod). Edge
  services (api-gateway, frontend) support Traefik-native weighted canary
  releases via the `canary` values block.
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - bakery
  - microservices
  - canary
home: https://github.com/YOUR_GITHUB_USERNAME/bakery-microservices
maintainers:
  - name: Crumb & Ember Platform Team
